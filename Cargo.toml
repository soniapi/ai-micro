[package]
name = "micro"
version = "0.1.0"
edition = "2024"

[dependencies]
diesel = { version = "2.2.0", features = ["postgres_backend", "postgres", "chrono"]}
infra = { path = "../infra" }
prop = { path = "../prop" }
dotenvy = "0.15"
